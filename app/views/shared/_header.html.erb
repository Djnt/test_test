<div class='top-header'>
  <div class='header-buttons'>
    <% if(user_signed_in?) %>
      <button  onClick="logout()" >Logout</button>
      <h2><%= current_user.email %><h2>
    <% else %>
      <a href="/users/sign_in"><button>Login/Register</button></a>
    <% end %>
  </div>
</div>